name: Package

on:
  push:
    branches: [main]

jobs:
  # JOB 1: Lint
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: "20", cache: "npm" }
      - run: npm ci
      - run: npm run lint

  # JOB 2: Build — hanya jalan kalau lint sukses
  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: "20", cache: "npm" }
      - run: npm ci
      - run: npm run build
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-info
          path: dist/build-info.json

  # JOB 3: Package — hanya jalan kalau build sukses, hanya di push ke main
  # Analog dengan package_job di GitLab CI:
  #   needs: [build_job]        -> needs: [build]
  #   npm run bundle            -> satukan src/ -> dist/bundle.js + manifest.json
  #   npm run budget            -> gerbang ukuran (performance budget)
  #   npm pack                  -> tarball versi = "paket siap-distribusi"
  #   artifacts expire_in: 1w  -> retention-days: 7
  #   rules: main only          -> dikontrol oleh trigger on.push.branches: [main]
  package:
    name: Package (main only)
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with: { node-version: "20", cache: "npm" }

      - name: Install dependencies
        run: npm ci

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: build-info
          path: dist/

      - name: Bundle (satukan src/ -> dist/bundle.js + manifest.json)
        run: npm run bundle

      - name: Budget check (gerbang ukuran / performance budget)
        run: npm run budget

      - name: Pack (tarball versi = paket siap-distribusi)
        run: npm pack

      - name: Upload package artifacts
        uses: actions/upload-artifact@v4
        with:
          name: app-${{ github.sha }}
          path: |
            dist/bundle.js
            dist/manifest.json
            *.tgz
          retention-days: 7
